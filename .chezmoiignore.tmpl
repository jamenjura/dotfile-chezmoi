.atl
.git
.zshrc.secrets
.zshrc_secrets

# Repository documentation lives in the repo only, it is not deployed
docs
README.md

# GitHub CLI tokens must never be managed
.config/gh/hosts.yml

{{ if eq .profile "work" -}}
# The personal modular zsh config is not deployed on work machines
.config/zsh/**

# Personal brew bundle
Brewfile
{{ end -}}
